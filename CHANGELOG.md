# 更新日志

中文 | [English](CHANGELOG_EN.md)

## [未发布]

## [0.1.2] - 2026-08-02

### 新增

- **应用内更新**：发现新版本时显示更新日志，由用户决定是否后台下载；安装包通过 Tauri 签名验证，下载完成后可选择立即或稍后安装

### 优化

- **刷新性能**：月度用量与 OpenCode Go 使用跨窗口短时缓存和并发请求合并；配额轮询与手动刷新自动复用进行中的请求，面板初始化和四类数据刷新改为并行执行
- **渲染性能**：配额倒计时共享时钟，卡片与模型趋势组件减少无效重渲染，折线图悬停时不再重复计算静态路径，屏外卡片延迟绘制
- **界面细节**：统一面板间距、交互焦点、设置项分组和数字对齐，并修正模型趋势折线与图例文字未对齐的问题

## [0.1.1] - 2026-08-02

### 新增

- **OpenCode Go 订阅卡片**：通过 Workspace ID 与 `auth` Cookie 在线校验并读取官方 Go 额度页，在一张卡片中显示 5 小时、周、月三档额度的剩余百分比、美元金额、按欧洲央行每日参考汇率折算的人民币估值与重置倒计时；凭证存系统钥匙串，卡片及内部三档额度均支持显隐与排序

### 优化

- **刷新性能**：Kimi 配额、本机用量、月度用量与 OpenCode Go 改为并行刷新；人民币汇率采用 stale-while-revalidate，缓存立即返回、过期后后台更新，首次无缓存最多等待 2 秒
- **Kimi 订阅卡片**：本周、5 小时、本月与加油包四张独立卡合并为与 OpenCode Go 一致的紧凑组合卡片；旧排序自动迁移，四项数据可单独显隐并调整内部顺序
- **桌面小部件**：改为直接复用 Kimi 与 OpenCode Go 订阅组合卡片，额度行显隐/排序与主面板同步；旧的本月、周、5 小时小部件配置自动合并迁移
- **卸载清理**：Windows 发布统一使用 NSIS；卸载时清理开机启动残留，勾选“删除应用数据”后同时删除 KimiCodeBar 配置与 Windows 凭据，并严格保留 Kimi CLI 共享的 `~/.kimi-code`、`KIMI_CODE_HOME` 和会话数据

## [0.1.0] - 2026-07-31

首个公开发布版本（Windows）。

### 新增

- **托盘监控**：左键弹出面板、右键菜单（控制台/刷新/设置/退出），悬停 tooltip 显示完整配额；任一窗口剩余 <20% 托盘图标变红
- **配额面板**：本周用量、5 小时用量（剩余百分比 + 进度条 + 重置倒计时）、加油包余额
- **本月用量**：通过网页端 `kimi-auth` cookie 接入控制台同款接口，显示月度已用百分比与 Kimi Code 占比
- **登录方式**：OAuth Device Flow 浏览器一键授权，或 API Key（可在设置中切换）；OAuth 凭证 DPAPI 加密存储，API Key 与网页端令牌存 Windows 凭据管理器
- **本机用量统计**：今日/昨日 Token 消耗、最近 7 天柱状图、按模型 Top 5、缓存命中率（今日/7 天/每日/各模型）；支持按含供应商的完整配置别名识别并标注 `SECONDARY_MODEL`
- **模型趋势卡片**：同图对比多个模型最近 7 天每日趋势，可切换 Token 用量 / 缓存命中率并按天悬停查看明细
- **面板卡片排序**：设置页支持对全部业务卡片执行上移/下移，顺序持久化且与显隐开关独立
- **会话自动归档**：按一天/一周/一月阈值自动归档，支持手动归档与取消归档
- **技能管理**：浏览 `~/.kimi-code/skills` 技能定义与全文
- **更新检查**：Kimi Code CLI 新版本检测（`kimi --version` 对比官方 changelog）与应用自身更新检查（GitHub Releases）
- **设置窗口**：登录方式、主题（深/浅/跟随系统）、语言（中/英/跟随系统）、开机自启、刷新间隔、面板卡片显隐
- **界面体验**：卡片入场 stagger 动画、进度条/柱状图生长与持续条纹动效（`prefers-reduced-motion` 自动降级）、刷新 shimmer 加载、滚动条滚动时显示、深色/浅色双主题
